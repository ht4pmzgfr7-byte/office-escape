{
  "name": "office-escape",
  "version": "0.1.0",
  "description": "Office Escape - a humorous 2D side-scrolling runner built with Phaser 3, TypeScript and Vite",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  }
}
